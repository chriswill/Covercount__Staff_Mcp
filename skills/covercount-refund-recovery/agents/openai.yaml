interface:
  display_name: "CoverCount Refund Recovery"
  short_description: "Check and recover cancellation refunds"

dependencies:
  tools:
    - type: "mcp"
      value: "covercount"
      description: "CoverCount staff reservation and event tools"
      transport: "streamable_http"
      url: "https://mcp.covercount.io/mcp"
