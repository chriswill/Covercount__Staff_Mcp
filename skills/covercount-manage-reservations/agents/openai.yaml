interface:
  display_name: "CoverCount Reservations"
  short_description: "Find, book, change, and cancel reservations"

dependencies:
  tools:
    - type: "mcp"
      value: "covercount"
      description: "CoverCount staff reservation and event tools"
      transport: "streamable_http"
      url: "https://mcp.covercount.io/mcp"
