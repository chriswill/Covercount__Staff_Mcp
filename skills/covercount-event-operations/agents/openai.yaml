interface:
  display_name: "CoverCount Event Operations"
  short_description: "Event briefings, financials, and operations"

dependencies:
  tools:
    - type: "mcp"
      value: "covercount"
      description: "CoverCount staff reservation and event tools"
      transport: "streamable_http"
      url: "https://mcp.covercount.io/mcp"
