interface:
  display_name: "CoverCount Guest Messages"
  short_description: "Draft and send guest email or SMS messages"

dependencies:
  tools:
    - type: "mcp"
      value: "covercount"
      description: "CoverCount staff reservation and event tools"
      transport: "streamable_http"
      url: "https://mcp.covercount.io/mcp"
